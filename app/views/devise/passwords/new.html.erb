<div class="bg-white rounded shadow-md p-8">
  <h2 class="text-2xl font-bold mb-6">Redefinir Senha</h2>
  <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: "mb-6" }) do |f| %>
    <div class="mb-4">
      <%= f.label :email, class: "block text-gray-700 font-bold mb-2" %>
      <%= f.email_field :email, autofocus: true, autocomplete: "email", class: "appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" %>
    </div>
    <div class="flex items-center justify-between">
      <%= f.submit "Enviar link de redefinição de senha", class: "bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" %>
      <%= link_to "Voltar para o Login", new_session_path(resource_name), class: "text-purple-500 hover:text-purple-700 font-bold" %>
    </div>
  <% end %>
</div>